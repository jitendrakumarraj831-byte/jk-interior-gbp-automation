import SocialHistoryClient from './history-client';

export const dynamic = 'force-dynamic';
export const metadata = { title: 'Social History' };

export default function SocialHistoryPage() {
  return <SocialHistoryClient />;
}
